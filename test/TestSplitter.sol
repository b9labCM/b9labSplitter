pragma solidity ^0.4.2;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Splitter.sol";

contract TestSplitter {

  /*function testInitialBalanceUsingDeployedContract() public {
    Splitter splitter2Test = Splitter(DeployedAddresses.Splitter());

    uint expected = 0;

    Assert.equal(splitter2Test.getBalance(tx.origin), expected, "Balance owner should be zero");
  }

  function testInitialBalanceWithNewMetaCoin() public {
    Splitter splitter2Test = new Splitter();

    uint expected = 0;

    Assert.equal(splitter2Test.getBalance(tx.origin), expected, "Balance owner should be zero");
  }
   */
}
